// Example unit test — replace with tests for your game logic.
// Convention: test files live in __tests__/ and end with .test.ts

describe('example', () => {
  it('passes', () => {
    expect(1 + 1).toBe(2);
  });

  // TODO: import and test your managers/models
  // e.g.:
  // import { createRoom } from '../server/src/models/room';
  // describe('createRoom', () => { ... });
});
